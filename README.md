# USDC Asset Handler

## Setup

```
git submodule init
git submodule update --init --recursive

cd lib/usdc-token
yarn set version 1.22.19
yarn
yarn install

cd lib/era-contracts
yarn 
git submodule init
git submodule update --init --recursive
```